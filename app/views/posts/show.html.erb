<div class="flex flex-col gap-3">
  <div class="p-4 border border-black border-solid">
    <h3><%= @post.title %></h3>
    <p><%= @post.text %></p>
  </div>
    <%= render partial:'shared/post_comments', locals: { comments: @post.comments } %>
</div>