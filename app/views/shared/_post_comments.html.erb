<ul class="flex flex-col gap-3 w-full p-4 border border-black border-solid">
  <p class="only:block hidden">There are no comments yet!</p>
  <% comments.each do |comment| %>
    <li> <%= comment.author.name %> :  <%= comment.text %></li>
  <% end %>
</ul>