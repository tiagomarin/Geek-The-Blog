<li class ="flex flex-col gap-3 w-full hover:bg-gray-100 border border-black border border-gray-400 rounded shadow md:flex-row ">
  <img class="w-full md:w-40" src="<%= user.photo %>">
  <div class="flex justify-between items-center p-4 w-full">
    <h3 class="text-xl"><%= user.name %></h3>
    <div class="flex self-end gap-5">
      <span>nº of posts: <%= user.posts.length %></span>
      <%# if can? :manage, User %>
        <%#= button_to "", users_path(user), :method => :delete, class:"fa-solid fa-trash-can " %>
      <%# end %>
    </div>
  </div>
</li>